FileInputStream fis = new FileInputStream(Path.of(mainCertProperties.getPath()).toFile());
        CertificateFactory cf = CertificateFactory.getInstance("X.509");
        X509Certificate cert = (X509Certificate) cf.generateCertificate(fis);

        // Podesavanje sifre na sertifikat
        KeyStore ks = KeyStore.getInstance("PKCS12");
        ks.load(null, null);
        ks.setCertificateEntry("Singidunum CA", cert);

        char[] password = "lozinka123".toCharArray();
        try (FileOutputStream fos = new FileOutputStream("certs/main/main.p12")) {
            ks.store(fos, password);
        }
System.out.println("Sertifikat je uspešno dodat u keystore!");